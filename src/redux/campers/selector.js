export const selectCampersItems = (state) => state.campers.items;
export const selectSelectedCamper = (state) => state.campers.selectedCamper;
export const selectCampersLoading = (state) => state.campers.loading;
export const selectCampersLoadingMore = (state) => state.campers.loadingMore;
export const selectCampersError = (state) => state.campers.error;
export const selectCampersTotal = (state) => state.campers.total;
export const selectCampersPage = (state) => state.campers.page;
export const selectCampersPerPage = (state) => state.campers.perPage;
